# Placeholder para el build de producción (npm run build).
# El compañero de infra define/inyecta la URL real del API (p. ej. la ruta que exponga
# el backend detrás de CloudFront/Nginx/API Gateway). Ruta relativa por defecto:
VITE_API_URL=/api/tasks
